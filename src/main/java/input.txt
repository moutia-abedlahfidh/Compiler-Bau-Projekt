1 + 2 * 3
# assign
x = 5; x + 1

# if true / false
if (1 < 2) x = 5; else x = 6; x


# while
x = 0; while (x < 3) x = x + 1; x

# for
x = 0; for (i = 0; i < 3; i = i + 1) x = x + i; x

# nested for
x = 0; for (i = 0; i < 3; i = i + 1) { for (j = 0; j < 2; j = j + 1) { x = x + i + j } } x

# function call
function add(a, b) { return a + b } add(2, 3)

# recursion factorial
function fact(n) { if (n <= 1) return 1; return n * fact(n - 1) } fact(5)

# comparisons starting with identifier
x = 1; x < 2

# parenthesized comparison
(1 < 2)

# comparison inside expression
x = 1; (x < 2) + 1

# function without explicit return
function id(a) { a } id(5)
function add(a, b) { return a + b }
# operator precedence and associativity
1 - 2 - 3
1 + 2 * 3
(1 + 2) * 3
-(1 + 2)
+3

# division by zero
1 / 0

# undefined variable
y + 1

# empty input and comment-only lines

// comment only line

# empty block and if without else
if (1) { }
if (0) x = 1; x

# for variants (missing parts)
for (; 0; ) x = 1
for (i = 0; ; i = i + 1) { if (i > 2) return i }

# type checker errors
if (1) x = 1
while (2) x = 1
x = 1 < 2; x + 1
function isSmall(a) { return a < 1 } isSmall(2) + 1

# comments only
// just a comment
/* block comment example */

# parse error (unbalanced brace)
x = 0; while (x < 3) { x = x + 1

# example with print
a = 7; b = 3;
c= a + b ; c

# comparisons eq/neq
1 == 1
1 != 2
1 == 2

# functions: zero-arg, return without expr, arity errors
function zero() { return 7 } zero()
function emptyReturn() { return; } emptyReturn()
function one(a) { return a } one()
function one2(a) { return a } one2(1, 2)
missingFunc(1)

# exponent numbers
1e3 + 2
3.5e-2 + 1

# nested function calls
function inc(a) { return a + 1 } inc(inc(1))

